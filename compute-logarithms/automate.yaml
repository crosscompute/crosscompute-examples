---
crosscompute: 0.9.4
name: Compute Logarithms
version: 0.0.1
input:
  variables:
    - id: base
      view: number
      path: variables.dictionary
    - id: start
      view: number
      path: variables.dictionary
    - id: stop
      view: number
      path: variables.dictionary
    - id: step
      view: number
      path: variables.dictionary
output:
  variables:
    - id: logarithms
      view: table
      path: values.json
  templates:
    - path: output.md
tests:
  - folder: tests/base-e
  - folder: tests/base-10
presets:
  - folder: presets/base-2
  - folder: presets/base-e
  - folder: presets/base-10
scripts:
  - path: run.ipynb
display:
  styles:
    - path: style.css
  pages:
    - id: output
      design: none
